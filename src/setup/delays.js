/**
 * Delay for the next button press
 * @module ./setup/delays
 *
 */
const BTN_DELAY = 750;

/**
 * Delay for each button effect
 * @module ./setup/delays
 */
const EFFECT_DELAY = 375;

/**
 * A 1 second delay
 * @module ./setup/delays
 */
const SEC_DELAY = 1000;

export { BTN_DELAY, EFFECT_DELAY, SEC_DELAY };
